(*Errors fixed in code.*)
val x = 34

val y = x + 1

val z = if y > 0 then true else x < 4

val q = if y > 0 then 0 else 42

val a = ~5

val w = 0

val funny = 34

val v = x div (w + 1)

val fourteen = 7 + 7
